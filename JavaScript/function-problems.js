// 두 수를 받아서곱한 결과를 반환하는 함수 multiply를 만들어 보기
// 예: multiply(4, 5) => 20

function multiply(a, b) { // 곱하기 함수 정의
    return a * b;
}

console.log(multiply(4, 5)); // 곱셈결과 20 출력

function getIntro(name , age) { // 함수 정의
    return `저는 ${age}살 ${name}입니다.`
}

console.log(getIntro("민수", 25)); // 결과 출력

